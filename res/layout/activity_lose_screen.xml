<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/fail"
    tools:context=".LoseScreen" >

    <TextView
        android:id="@+id/LoseMessageTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:gravity="center_horizontal"
        android:text="@string/lose_message"
        android:textSize="@dimen/messageSize" />

    <Button
        android:id="@+id/HomeButton"
        android:layout_width="90dp"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/LoseMessageTextView"
        android:layout_marginBottom="18dp"
        android:layout_marginRight="92dp"
        android:background="@android:color/transparent"
        android:onClick="gotoHome" />

    <TextView
        android:id="@+id/scoreTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/loseScoreTextView"
        android:layout_alignBottom="@+id/loseScoreTextView"
        android:layout_marginRight="48dp"
        android:layout_toLeftOf="@+id/HomeButton"
        android:text="@string/score_textview"
        android:textSize="@dimen/textSize" />

    <TextView
        android:id="@+id/loseScoreTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/HomeButton"
        android:layout_below="@+id/LoseMessageTextView"
        android:layout_marginRight="17dp"
        android:layout_marginTop="21dp"
        android:text="@string/userScore"
        android:textSize="@dimen/textSize"
        android:textStyle="bold" />
    
</RelativeLayout>
